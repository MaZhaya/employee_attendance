<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.LeaveMapper">
    <insert id="addLeave">
        INSERT INTO leave_apply(emp_id, leave_type, start_time, end_time, reason, approve_status)
        VALUES(#{empId}, #{leaveType}, #{startTime}, #{endTime}, #{reason}, #{approveStatus})
    </insert>

    <update id="updateStatus">
        UPDATE leave_apply
        SET approve_status=#{status}
        WHERE id=#{id}
    </update>

    <select id="listAll" resultType="entity.LeaveApply">
        SELECT * FROM leave_apply
    </select>

    <select id="listByEmpId" resultType="entity.LeaveApply">
        SELECT * FROM leave_apply WHERE emp_id=#{empId}
    </select>
</mapper>